{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "paths": { "#pier/*": ["./src/.pier/*"] },
    "types": ["node", "bun", "aws-lambda"]
  },
  "include": ["scripts", "src", "sst-env.d.ts", "vite.config.ts"]
}
